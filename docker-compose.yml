version: '2'

services:
  nginx:
    image: nginx:alpine
    ports:
        - "80:80"
        - "443:443"
    build:
      context: ./client
      dockerfile: Dockerfile

  netcore:
    image: "netcore"
    volumes:
      - ./server:/var/www/server
    ports:
      - "5000:80"
    build:
      context: ./server
      dockerfile: Dockerfile